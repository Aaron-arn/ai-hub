# Skill: Debugging

Load `skill.md` into the agent's context to enable this skill.

## What it does

Provides a systematic debugging method: reproduce, read errors, isolate, fix the root cause, verify.

## Usage

Provide this skill's instructions to an AI agent before debugging work, for example by injecting `skill.md` into its system prompt.
