# Skill: API Integration

Load `skill.md` into the agent's context to enable this skill.

## What it does

Consume third-party APIs safely: auth, errors, pagination, retries.

## Usage

Provide this skill's instructions to an AI agent before asking it to work on api integration, for example by injecting `skill.md` into its system prompt.
