# Skill: Database Migrations

Load `skill.md` into the agent's context to enable this skill.

## What it does

Plan and write safe schema migrations with rollback strategy.

## Usage

Provide this skill's instructions to an AI agent before asking it to work on database migrations, for example by injecting `skill.md` into its system prompt.
