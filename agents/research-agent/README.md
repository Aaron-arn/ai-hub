# Research Agent

Plans multi-step research, gathers and cross-checks sources, and delivers
a structured cited report.

## Install

```bash
aihub install research-agent
```

The agent loads `agent.md` into the assistant's context. The
`aihub:web-search` and `aihub:wikipedia-summary` tool dependencies are
installed automatically when you install with dependencies enabled.
