# SQLite Query

Run read-only SELECT queries against a SQLite database.

## Usage

```bash
python main.py --help
```

## Permissions

- `network`: False
- `filesystem`: True
- `shell`: False
- `environment`: False
