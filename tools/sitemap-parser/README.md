# Sitemap Parser

Fetch and parse XML sitemaps into a list of URLs.

## Usage

```bash
python main.py --help
```

## Permissions

- `network`: True
- `filesystem`: False
- `shell`: False
- `environment`: False
