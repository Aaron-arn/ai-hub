# Date & Time

Current time, ISO formatting, unix timestamps and timezone conversions using the IANA timezone database.

## Usage

```bash
python main.py now
python main.py now "Europe/Paris"
python main.py unix
python main.py convert 1754496000 "Asia/Tokyo"
```

## Permissions

- No permissions required
